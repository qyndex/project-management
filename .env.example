# Project Management — environment variables
# Copy to .env.local and fill in values

# API base URL (leave empty to use relative paths in development)
VITE_API_BASE_URL=

# Feature flags
VITE_ENABLE_NOTIFICATIONS=false
